// Q. Write a Program to find the size of int, float, double, and char
#include <stdio.h>

int main() {
    printf("\nSize of int is %u.", sizeof(int));
    
    printf("\nSize of float is %u.", sizeof(float));
    
    printf("\nSize of double is %u.", sizeof(double));
    
    printf("\nSize of char is %u.", sizeof(char));

    return 0;
}